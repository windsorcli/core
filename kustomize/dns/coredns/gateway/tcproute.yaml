apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: coredns
  namespace: system-dns
spec:
  parentRefs:
    - name: external
      namespace: system-gateway
      sectionName: dns-tcp
  rules:
    - backendRefs:
        - name: coredns
          port: 53
