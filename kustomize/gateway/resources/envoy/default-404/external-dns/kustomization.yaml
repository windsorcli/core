apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
patches:
  - path: patches/route.yaml
    target:
      group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: default-404-${gateway_name}
      namespace: system-gateway
