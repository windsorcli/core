apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: external-web-tls
  namespace: system-gateway
spec:
  secretName: external-web-tls
  dnsNames:
    - "${external_domain}"
    - "*.${external_domain}"
  issuerRef:
    kind: ClusterIssuer
    name: ${gateway_cert_issuer}
