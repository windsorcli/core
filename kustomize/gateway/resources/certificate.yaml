apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: edge-web-tls
  namespace: system-gateway
spec:
  secretName: edge-web-tls
  dnsNames:
    - "*.${external_domain}"
  issuerRef:
    kind: ClusterIssuer
    name: private
