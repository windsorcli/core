apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ${gateway_name}-web-tls
  namespace: system-gateway
spec:
  secretName: ${gateway_name}-web-tls
  dnsNames:
    - "${gateway_domain}"
    - "*.${gateway_domain}"
  issuerRef:
    kind: ClusterIssuer
    name: ${gateway_cert_issuer}
