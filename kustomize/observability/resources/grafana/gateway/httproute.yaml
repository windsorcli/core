apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
  namespace: system-observability
spec:
  parentRefs:
    # web-https only; plain HTTP is 301-redirected by the gateway's https-redirect.
    # internal: Grafana is an operational surface, never internet-facing.
    - kind: Gateway
      name: internal
      namespace: system-gateway
      sectionName: web-https
  hostnames:
    - grafana.${internal_domain}
  rules:
    - backendRefs:
        - kind: Service
          name: grafana
          port: 80
