apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Fluentd dashboards - vendored
# Run: task dashboards
# See: source.yaml for provenance

configMapGenerator:
  - name: grafana-dashboards-fluentd
    namespace: system-observability
    files:
      - fluentd.json
    options:
      disableNameSuffixHash: true
      labels:
        grafana_dashboard: "1"
      annotations:
        grafana_folder: "Observability"
