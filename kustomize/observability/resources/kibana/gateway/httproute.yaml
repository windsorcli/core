apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kibana
  namespace: system-observability
spec:
  parentRefs:
    # web-https only; plain HTTP is 301-redirected by the gateway's https-redirect.
    # internal: Kibana is an operational surface, never internet-facing.
    - kind: Gateway
      name: internal
      namespace: system-gateway
      sectionName: web-https
  hostnames:
    - kibana.${internal_domain}
  rules:
    - backendRefs:
        - kind: Service
          name: kibana-kibana
          port: 5601
