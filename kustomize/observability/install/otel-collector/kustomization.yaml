apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# PROTOTYPE: investigating the native OTel access log path for Envoy Gateway
# (as an alternative to fluent-bit tailing Envoy's stdout JSON).

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: otel-collector-config
    namespace: system-observability
    files:
      - collector.yaml
