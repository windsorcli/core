apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# PROTOTYPE: investigating the native OTel access log path for Envoy Gateway
# (as an alternative to fluent-bit tailing Envoy's stdout JSON). Not yet
# wired into any facet - deploy and reference manually while validating.

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
