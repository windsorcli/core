apiVersion: v1
kind: Service
metadata:
  name: otel-collector
  namespace: system-observability
spec:
  selector:
    app.kubernetes.io/name: otel-collector
  ports:
    - name: otlp-grpc
      port: 4317
      targetPort: otlp-grpc
      protocol: TCP
