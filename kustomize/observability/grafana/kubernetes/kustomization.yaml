apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# DEPRECATED: This component will be removed in v0.8.0
# Use dashboards/kubernetes/ directly instead
# See: MOVED.md for migration details

components:
  - ../dashboards/kubernetes
