---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: quickwit
  namespace: system-observability
spec:
  interval: 5m
  timeout: 10m
  chart:
    spec:
      chart: quickwit
      # renovate: datasource=helm depName=quickwit package=quickwit helmRepo=https://helm.quickwit.io
      version: 0.7.20
      sourceRef: 
        kind: HelmRepository
        name: quickwit
        namespace: system-gitops
  values:
    image:
      # renovate: datasource=docker depName=quickwit/quickwit package=quickwit/quickwit
      tag: 0.7.20@sha256:89a052af62c612d0e05d2596f03edba77d7d904c4478b387a5dc6305821fe0a1
    searcher:
      replicaCount: 1
