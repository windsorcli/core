apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# CloudNativePG alerts, verbatim from the operator's own canonical set at
# cloudnative-pg/cloudnative-pg docs/src/samples/monitoring/alerts.yaml.
# samber/awesome-prometheus-alerts has no CloudNativePG section.

resources:
  - prometheus-rule.yaml
