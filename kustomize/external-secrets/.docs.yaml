# Descriptor consumed by scripts/kustomize-docs.sh to populate the
# BEGIN_KUSTOMIZE_DOCS / END_KUSTOMIZE_DOCS region of README.md.
# Edit this file (not the README tables) when substitutions, components,
# or dependencies change.

components:
  external-secrets:
    enable_when: "`external_secrets.enabled: true`"
    description: "Helm release of External Secrets Operator in `system-external-secrets`. Install-only: manages `ExternalSecret` and `ClusterSecretStore` CRs, creates none of its own. CRDs are vendored under `kustomize/crds/external-secrets-<version>/`, not chart-managed (`installCRDs: false`)."
