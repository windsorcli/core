apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
# No resources or components by default — openbao/gateway routes OpenBao's UI
# through the shared Gateway when secrets_store.driver is openbao and
# gateway.access is private.
