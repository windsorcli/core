apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
# One folder per SSO consumer: the realm-client patch registers the client in the platform
# realm import, and the Job copies Keycloak's generated secret into the consumer namespace.
patches:
  - path: realm-client.patch.yaml
resources:
  - job.yaml
