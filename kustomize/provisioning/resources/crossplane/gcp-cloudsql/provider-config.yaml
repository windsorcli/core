---
# Named "default": a consuming DatabaseInstance CR needs no
# providerConfigRef.
apiVersion: gcp.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
spec:
  projectID: ${crossplane_gcp_project_id}
  credentials:
    source: InjectedIdentity
