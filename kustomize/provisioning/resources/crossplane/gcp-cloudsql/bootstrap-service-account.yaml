---
# Identity the app-role and monitoring bootstrap CronJobs run as. Reads the
# admin credential database/gcp-cloudsql's Terraform already wrote to a
# Kubernetes Secret — no GCP identity of its own, since nothing here calls
# a GCP API.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cloudsql-bootstrap
  namespace: system-provisioning
