apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: flux-operator
  namespace: system-gitops
spec:
  parentRefs:
    # web-https only; plain HTTP is 301-redirected by the gateway's https-redirect.
    # internal: the Flux UI is an operational surface, never internet-facing.
    - kind: Gateway
      name: internal
      namespace: system-gateway
      sectionName: web-https
  hostnames:
    - flux.${internal_domain}
  rules:
    - backendRefs:
        - kind: Service
          name: flux-operator
          port: 9080
