apiVersion: v1
kind: Service
metadata:
  name: flux-webhook-receiver
  namespace: system-gitops
spec:
  type: LoadBalancer
  selector:
    app: notification-controller
  ports:
    - name: http
      port: 9292
      protocol: TCP
      targetPort: 9292
