apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Needs telemetry.metrics.enabled: PodMonitor (monitoring.coreos.com/v1)
# only exists when the metrics pipeline vendors prometheus-operator's CRDs.
#
# Driver-agnostic: annotations and the watched CRD are set by a driver's
# own monitoring-exporter sub-component.

resources:
  - policy.yaml
