apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# RDS's own ProviderConfig, tagging policy, and network defaults.

resources:
  - provider-config.yaml
  - tag-policy.yaml
  - network-policy.yaml
