apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Flexible Server's own ProviderConfig, resource-group policy, and
# tagging policy.

resources:
  - provider-config.yaml
  - resourcegroup-policy.yaml
  - tag-policy.yaml
