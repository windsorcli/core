apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Flexible Server's own ProviderConfig, resource-group policy, tagging
# policy, and network defaults.

resources:
  - provider-config.yaml
  - resourcegroup-policy.yaml
  - tag-policy.yaml
  - network-policy.yaml
