apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Cloud SQL's own ProviderConfig, project policy, and tagging policy.

resources:
  - provider-config.yaml
  - project-policy.yaml
  - tag-policy.yaml

patches:
  # No-op unless the caller also includes crossplane/postgres/monitoring-exporter.
  - path: patches/monitoring-exporter-policy.yaml
    target:
      kind: ClusterPolicy
      name: instance-monitor-exporter
