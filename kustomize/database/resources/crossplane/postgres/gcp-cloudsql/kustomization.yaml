apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Cloud SQL's own ProviderConfig, project policy, tagging policy, and
# network defaults.

resources:
  - provider-config.yaml
  - project-policy.yaml
  - tag-policy.yaml
  - network-policy.yaml
