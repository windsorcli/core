kind: Facet
apiVersion: blueprints.windsorcli.dev/v1alpha1
metadata:
  name: observability
  description: Observability stack with metrics backends, log storage drivers, and dashboarding

when: addons.observability.enabled == true

# =============================================================================
# Config — dashboard backend default
# =============================================================================
# Land `addons.observability.dashboards` so other facets can read it without
# re-applying the fallback. logs_driver has no default — unset means "no external
# log storage, fluentd aggregator only".
config:

  - name: addons
    value:
      observability:
        dashboards: "${addons.observability.dashboards ?? 'grafana'}"

  # Derived external Grafana URL. The identity facet reads this to set the OIDC
  # client's redirect URI, and grafana/oidc uses it for server.root_url. docker-desktop
  # reaches the gateway on :8443 (its NodePort host-forward); elsewhere on :443.
  # Effective client secret: the supplied value, or a dev throwaway so local SSO is
  # zero-config (mirrors the dev Grafana admin password). Written into both the client
  # CR's namespace (identity facet) and Grafana's (below), so the copies match.
  - name: grafana_effective
    value:
      url: >-
        ${'https://grafana.' +
        ((dns.public_domain ?? '') == '' ? (dns.private_domain ?? '') : dns.public_domain) +
        (workstation.runtime == 'docker-desktop' ? ':8443' : '')}
      client_secret: >-
        ${(addons.observability.grafana.client_secret ?? '') != '' ?
        addons.observability.grafana.client_secret :
        (dev == true ? 'grafana-oidc-dev' : '')}

requires:
  # Grafana SSO needs a cluster identity provider to authenticate against.
  - when: addons.observability.grafana.sso == true
    paths:
      - identity.enabled
    message: Grafana SSO authenticates against the cluster identity provider — enable identity.
  # A client secret must be supplied outside dev — an external IdP always owns it, and
  # the hosted provider no longer generates one. Dev fills a throwaway automatically.
  - when: addons.observability.grafana.sso == true && (identity.driver == 'oidc' || dev != true)
    paths:
      - addons.observability.grafana.client_secret
    message: Grafana SSO needs a client secret — set addons.observability.grafana.client_secret (a ${secret(...)} ref).

# =============================================================================
# Flux systems — observability install entries, telemetry override
# =============================================================================
# Every entry merges into one `observability` system: HelmReleases land in the
# install tier, dashboards/routes/fluentd config in the resources tier. Other
# facets patch in their own dashboards under `- name: observability` entries.
flux:

  # When Elasticsearch is the log store, replace platform-base's telemetry system:
  # swap fluent-bit for filebeat (native ES shipper) and drop the fluentbit stack.
  - name: telemetry
    when: addons.observability.logs_driver == 'elasticsearch' && gateway.enabled == true
    strategy: replace
    install:
      components:
        - prometheus
        - prometheus/flux
        - filebeat
        - metrics-server
      timeout: 30m
      interval: 10m
    resources:
      - components:
          - prometheus
          - prometheus/flux
          - "${telemetry.alert_components ?? []}"
        timeout: 10m
        interval: 5m

  # Base Grafana + standard dashboards. Substitutions propagate timezone/time
  # format to panel defs so timestamps match operator locale.
  - name: observability
    when: addons.observability.dashboards == 'grafana'
    dependsOn:
      - telemetry-install
      - "${dns.enabled ? 'dns-install' : ''}"
    install:
      timeout: 15m
      interval: 10m
      components:
        - grafana
        - grafana/prometheus
        # Single sign-on against the cluster identity provider. Provider-agnostic:
        # the issuer comes from identity_effective, the client id/role from grafana.*.
        - "${addons.observability.grafana.sso == true ? 'grafana/oidc' : ''}"
      substitutions:
        external_domain: "${(dns.public_domain ?? '') == '' ? (dns.private_domain ?? '') : dns.public_domain}"
        grafana_root_url: "${grafana_effective.url}"
        # generic_oauth inputs — the provider label, issuer, and endpoint overrides come
        # from identity (one place, both drivers); endpoints default to the issuer's
        # standard OIDC path. Client id and role mapping from grafana.*. All leaves are
        # null-safe, so these stay valid when SSO is off. Role mapping defaults to
        # platform-admins → Grafana Admin.
        oidc_name: "${identity_effective.display_name ?? 'SSO'}"
        oidc_client_id: "${addons.observability.grafana.client_id ?? 'grafana'}"
        oidc_auth_url: "${(identity.auth_url ?? '') != '' ? identity.auth_url : ((identity_effective.issuer ?? '') + '/protocol/openid-connect/auth')}"
        oidc_token_url: "${(identity.token_url ?? '') != '' ? identity.token_url : ((identity_effective.issuer ?? '') + '/protocol/openid-connect/token')}"
        oidc_userinfo_url: "${(identity.userinfo_url ?? '') != '' ? identity.userinfo_url : ((identity_effective.issuer ?? '') + '/protocol/openid-connect/userinfo')}"
        oidc_role_attribute_path: "${addons.observability.grafana.role_attribute_path ?? \"contains(groups[*], '/platform-admins') && 'Admin' || 'Viewer'\"}"
    resources:
      - timeout: 10m
        interval: 5m
        components:
          - grafana/dashboards/node
          - grafana/dashboards/kubernetes
          - grafana/dashboards/flux
          - grafana/dashboards/cert-manager
          - grafana/dashboards/fluent-bit
          - grafana/dashboards/fluentd
        substitutions:
          timezone: ${timezone}
          date_full: "${time_format == '12h' ? 'MMM DD, YYYY @ hh:mm:ss a' : 'YYYY-MM-DD HH:mm:ss'}"
          date_interval_second: "${time_format == '12h' ? 'hh:mm:ss a' : 'HH:mm:ss'}"
          date_interval_minute: "${time_format == '12h' ? 'hh:mm a' : 'HH:mm'}"
          date_interval_hour: "${time_format == '12h' ? 'MMM DD hh:mm a' : 'MM/DD HH:mm'}"
          date_interval_day: "${time_format == '12h' ? 'MMM DD' : 'MM/DD'}"

  # Grafana OIDC client secret in Grafana's namespace, read by generic_oauth
  # (grafana/oidc). The effective value is the supplied client_secret or a dev
  # throwaway; the identity facet writes the same value into the client CR's namespace.
  - name: observability
    when: addons.observability.grafana.sso == true
    secrets:
      grafana-oidc-client:
        data:
          clientSecret: ${grafana_effective.client_secret}

  # Gateway route + Envoy-specific dashboards when the cluster has a Gateway.
  # Waits for gateway-resources so the Gateway exists before the HTTPRoute applies.
  - name: observability
    when: addons.observability.dashboards == 'grafana' && gateway.enabled == true
    resources:
      - dependsOn:
          - gateway-resources
        components:
          - grafana/gateway
          - "${gateway.driver == 'envoy' ? 'grafana/dashboards/envoy' : ''}"
        substitutions:
          external_domain: "${(dns.public_domain ?? '') == '' ? (dns.private_domain ?? '') : dns.public_domain}"

  # =============================================================================
  # Log storage drivers — one block fires per logs_driver value
  # =============================================================================

  # stdout: write logs to container stdout (no external store). Dev default.
  - name: observability
    when: addons.observability.logs_driver == 'stdout'
    resources:
      - dependsOn:
          - telemetry-resources
        components:
          - fluentd/outputs/stdout

  # Quickwit: search-optimized log store on object storage (PVC for staging).
  # Install waits for csi (staging PVC).
  - name: observability
    when: addons.observability.logs_driver == 'quickwit'
    dependsOn:
      - csi
    install:
      timeout: 20m
      interval: 5m
      components:
        - quickwit
        - quickwit/pvc
        - quickwit/prometheus
        - "${addons.observability.dashboards == 'grafana' ? 'grafana/quickwit' : ''}"
    resources:
      - dependsOn:
          - telemetry-resources
        timeout: 10m
        interval: 5m
        components:
          - fluentd/outputs/quickwit
          - "${addons.observability.dashboards == 'grafana' ? 'grafana/dashboards/logs/quickwit' : ''}"
          - "${addons.observability.dashboards == 'grafana' ? 'grafana/dashboards/quickwit' : ''}"

  # Elasticsearch + Kibana. Gated on gateway.enabled (Kibana needs an HTTPRoute
  # to be reachable). Install waits for csi (ES data PVCs).
  - name: observability
    when: addons.observability.logs_driver == 'elasticsearch' && gateway.enabled == true
    dependsOn:
      - csi
    install:
      timeout: 20m
      interval: 5m
      components:
        - elasticsearch
        - kibana
    resources:
      - dependsOn:
          - gateway-resources
        timeout: 10m
        interval: 5m
        components:
          - kibana/gateway
        substitutions:
          external_domain: "${(dns.public_domain ?? '') == '' ? (dns.private_domain ?? '') : dns.public_domain}"
