kind: Facet
apiVersion: blueprints.windsorcli.dev/v1alpha1
metadata:
  name: dev
  description: "Development mode configurations including selfsigned certificates and dev-specific resources"

when: dev == true

config:
# In dev mode, auto-enable common addons unless explicitly disabled.
- name: addons
  value:
    private_ca:
      enabled: ${addons.private_ca.enabled != false}
    private_dns:
      enabled: ${addons.private_dns.enabled != false}
    observability:
      enabled: ${addons.observability.enabled != false}
    database:
      postgres:
        enabled: ${addons.database.postgres.enabled != false}
        driver: "${addons.database.postgres.driver ?? 'cloudnativepg'}"

kustomize:

# PKI resources for development (selfsigned issuer)
- name: pki-resources
  path: pki/resources
  dependsOn:
  - pki-base
  components:
  - public-issuer/selfsigned

# Grafana dev credentials
- name: observability
  path: observability
  dependsOn:
    - telemetry-base
  components:
    - grafana/dev
  substitutions:
    admin_password: ${addons.observability.admin_password ?? "grafana"}
