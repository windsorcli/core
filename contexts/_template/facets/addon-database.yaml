kind: Facet
apiVersion: blueprints.windsorcli.dev/v1alpha1
metadata:
  name: database
  description: PostgreSQL database service with CloudNativePG

when: addons.database.postgres.enabled == true

kustomize:

# CloudNativePG operator base
- name: database
  path: database
  when: addons.database.postgres.driver == 'cloudnativepg'
  timeout: 15m
  interval: 10m
  dependsOn:
    - csi
  components:
    - cloudnativepg
    - cloudnativepg/prometheus
    - "${addons.database.postgres.driver == 'cloudnativepg' && ha == true ? 'cloudnativepg/ha' : ''}"

# CloudNativePG Grafana dashboard
- name: observability
  path: observability
  when: addons.database.postgres.driver == 'cloudnativepg' && addons.observability.enabled == true
  dependsOn:
    - telemetry-base
  components:
    - grafana/dashboards/cloudnativepg
