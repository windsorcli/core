pre-commit:
  parallel: true
  commands:
    tofu-fmt:
      glob: "*.{tf,tfvars}"
      run: tofu fmt {staged_files}
      stage_fixed: true

pre-push:
  parallel: true
  commands:
    tofu-test:
      glob: "*.{tf,tfvars}"
      run: task test
